{
  "name": "dash-hangout-mute",
  "version": "0.0.1",
  "description": "Mutes and unmutes the classic hangout using an amazon dash button (and the dhcp protocol)",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": {
    "name": "CJ Buchmann"
  },
  "license": "MIT",
  "dependencies": {
    "robotjs": "^0.4.4",
    "dhcpjs":"^0.5.0"
  }
}
